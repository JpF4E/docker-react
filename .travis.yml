language: generic
sudo: required
services:
        - docker

before_install:
        - docker build -t jpf4e/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true jpf4e/docker-react npm run test
